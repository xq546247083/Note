# AspNetCore

    1、安装Node
    2、忽略Node的版本，运行：yarn config set ignore-engines true
    3、使用Developer Command Prompt for VS 2022运行restore.cmd

# Runtime

    1、安装Python3
    2、使用VSCode运行eng/build.ps1

# Abp

    1、用PowerShell运行build/build-all.ps1